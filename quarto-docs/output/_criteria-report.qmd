```{r, echo = FALSE}
status <- input_data |> filter(id == x) |> pull(status_label)

note <- input_data |> filter(id == x) |> pull(note)
```

### `r criteria_list[x]`

::: {.callout-tip collapse="true"}
## What does this mean?

```{r, results = "asis"}
a <- knitr::knit_child(paste0("_", x, ".qmd"),
                       envir = environment(),
                       quiet = TRUE)

cat(a, sep="\n")
```
:::

**Status**: `r status`

`r if(note != "") paste("**Notes**:", note)`

```{r, echo = FALSE}
htmltools::hr()
```