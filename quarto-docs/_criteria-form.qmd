### `r criteria_list[criteria_id]`

::: {.callout-tip collapse="true"}
## What does this mean?

```{r, results = "asis"}
a <- knitr::knit_child(
  paste0("help/_", criteria_id, ".qmd"), 
  envir = environment(), 
  quiet = TRUE
)

cat(a, sep="\n")
```
:::

```{r}
tagList(
  form_ui(criteria_id, 
          criteria_list[criteria_id], 
          options),
  hr()
)
```